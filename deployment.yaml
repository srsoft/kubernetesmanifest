apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      name: go
  template:
    metadata:
      labels:
        name: go
    spec:
      containers:
      - image: harbor.ks.io:8443/ceresinventory/go:22
        name: go-container
        ports:
        - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: go-service
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    name: go