apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: examplenode
  name: examplenode
spec:
  replicas: 3
  selector:
    matchLabels:
      app: examplenode
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: examplenode
    spec:
      containers:
      - image: harbor.ks.io:8443/example/node:20
        name: examplenode
        resources: {}
status: {}
---
apiVersion: v1
kind: Service
metadata:
  name: lb-service
  labels:
    app: lb-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: examplenode